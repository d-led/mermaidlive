import { Given } from "@cucumber/cucumber";

Given("a system in state {string}", async function (string) {
  // Write code here that turns the phrase above into concrete actions
  return "pending";
});
